# Fangcun Next Repository Instructions

本文件适用于整个仓库及其所有子目录。任何开发 Agent 在修改文件前都必须遵守。

## 项目目标

将旧 Fangcun 重构为一个以编剧为最终作者、AI 为可控创作助手的小说转短剧 Skill 系统。新版必须兼容多种 AI Agent 和模型，并把 OpenClaw、飞书等能力隔离在平台适配层。

## 规范来源

- 新版唯一设计基线：`docs/specs/fangcun-next-design-spec.md`；
- 开发、独立评审与红黄绿放行机制：`docs/governance/development-review-workflow.md`；
- 当前开发任务的范围、外部验收案例和完成定义，由启动提示词指定的 `docs/development/independent-review-roundN-fix-brief.md` 约束；
- 旧代码是迁移输入，不是新版需求来源；
- `docs/research/` 是问题证据，不是全题材统一写法；
- 旧版运行体系（`skills/`、`tools/`、旧 `agents/*.md`、`tests/drama/`、`docs/legacy|superpowers`）已迁出到只读归档，仅通过归档目录或 Git 历史溯源；迁移兼容层保留在 `scripts/migration.py`、legacy-scriptitem 适配与 `legacy_unspecified` 字段中；
- 用户在当前任务中的明确决定高于仓库文档。

修改前必须完整读取与当前任务相关的规范章节。涉及整体架构、数据契约或优先级时，应完整阅读规格书。

`AGENTS.md` 只作为全仓长期规则和文档地图，不复制当轮修复合同。开发模型负责实现和自测，不能自行宣布独立验收通过；外部验收案例不得删除、跳过、弱化或反向修改，除非当前修复合同明确指出既有测试与上位规格冲突。

## 已确认且不得擅自改变的业务决策

1. 不用单一剧本样本或固定类型流程约束所有题材；
2. 首轮保留改编指引和故事大纲，重点重构集纲到剧本；
3. 编剧拥有最终决定权；
4. 容量和时长估算只提供预期，不是自动回退门禁；
5. 一般时长要求是下限，不要求精确命中；
6. 小范围修改就地处理；是否整体重做由编剧明确决定；
7. 编剧确认的最新剧本是已发生剧情的最高事实来源；
8. 首轮不把 Token 统计、主 Bot 管理和公司运营报表混入创作核心；
9. 业务剧本格式、内部结构化数据和平台传输格式必须分离；
10. Writer、Reviewer 和 Rewriter 必须消费同一份不可变 `episode_context` 快照。

## 实施顺序

默认按以下顺序开发，除非用户明确调整：

1. 建立旧版行为基线和回归样本；
2. 实现数据 Schema 与 `episode_contract`；
3. 实现原文锚点检索和 `context_builder`；
4. 重构 Writer Prompt 和类型 Craft 路由；
5. 实现同源审核与定向重写；
6. 实现编剧定稿、版本和连续性回灌；
7. 完成 EP001 回归与多类型冒烟测试；
8. 实现容量与时长估算；
9. 最后接入 OpenClaw、飞书和运营能力。

## 代码与内容边界

- 用确定性代码处理文件、Schema、状态、版本、原文锚点、格式和连续性合并；
- 用模型处理需要创作判断、语义判断和类型表达的部分；
- 不把整套系统塞入一个超长 Prompt；
- `SKILL.md` 保持精简，详细方法放入一层可发现的 references；
- 不重复维护同一规则的多份副本；
- 不按固定前若干字符、固定章节平均分配方式截取原文；
- 不允许格式校验结果冒充剧情、台词或连续性审核结果。

## 测试要求

- 每个确定性模块必须有单元测试；
- 每次修复质量问题都应增加可复现回归样本；
- EP001 用于验证原文贴合、人物认知、台词连接、因果桥和格式，不要求逐字复制编剧稿；
- 至少覆盖多种题材的冒烟测试，避免对单一样本过拟合；
- 无 API 时必须明确标记降级，不能声称完成了系统模型审核；
- 不得把未获编剧确认的草稿自动提升为定稿或继续扩写后续批次。

## 安全与 GitHub 发布

- 不执行真实飞书写入或修改线上文档，除非用户明确授权并指定目标；
- 不提交 `.env`、API 密钥、飞书凭据、本地配置、小说原文和项目运行产物；
- 不提交 `projects/`、`_cache/`、`output/`、`logs/`、数据库、媒体或压缩包；
- 不修改当前生产版 `fangcun-v2-dev`；
- 不进行破坏性 Git 操作；
- 保留迁移兼容层时，明确标注 deprecated 和替代路径，不静默删除旧接口。

## 开发交付标准

每个实施阶段完成时都要报告：

- 修改了什么；
- 为什么符合规格书；
- 实际运行了哪些测试；
- 测试是否使用真实模型/API；
- 与旧版兼容性变化；
- 尚未解决的问题和下一步。

只有规格书中的验收条件实际通过，才能宣称相应阶段完成。
