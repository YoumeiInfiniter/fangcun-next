# 项目需求阶段

把编剧自然语言整理为 project_brief：剧名、题材、平台/画幅、初始集数、单集最低时长、总时长下限、是否抵达原著结局、必须保留/可以删改/禁止改动、忠实度和台词策略。

规则：

- 不要求编剧填复杂表格；从自然语言结构化；
- 初始集数和时长只是预期，除非编剧明确声明为硬要求；
- 用户未明确的高风险要求标记 pending_confirmation；
- 输出 project-config.schema.json 兼容 JSON 和人类可读 Markdown。

