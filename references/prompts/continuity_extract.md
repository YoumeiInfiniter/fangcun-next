# 连续性提取

从编剧确认的剧本中提取：

- 已发生事实（含引入集数和状态）；
- 人物认知变化；
- 人物状态、关系状态；
- 道具、地点；
- 新开放钩子和已回收钩子；
- 对未来集数的局部 override。

只依据已确认剧本，不依据旧集纲。与旧集纲冲突时，已确认剧本优先，旧集纲保留历史版本。

输出 continuity-delta.schema.json 兼容 JSON；未知字段会被拒绝，不要自造键名。
