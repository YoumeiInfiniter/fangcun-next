---
name: fangcun
description: |
  小说转短剧剧本改编引擎。
  触发方式：「用fangcun」「fangcun项目初始化」「写剧本」「改编短剧」「分析这本书」
---

# fangcun：短剧改编引擎

## 🚫 飞书平台铁律

**禁止在飞书中以任何形式开启话题（Thread）。** 所有消息（产出物、审核报告、进度通知、确认询问等）必须直接发送在主对话中。原因：话题模式下智能体无法获取上文，导致上下文断裂、对话无法接续。违反此规则会导致流程失控。

## 标准入口流程（拉群后的理想路径）

Fangcun 进入新飞书项目群后，必须按三段式推进，不能合并步骤：

1. **项目初始化**：用户先说「用 fangcun」「fangcun 项目初始化」「初始化项目」等。此时只做初始化：抓取当前飞书群名 → 在人马剧本库下创建/复用「群名项目文件夹」→ 创建/复用表1、表2 → 在主群回贴项目文件夹、表1、表2三个链接 → 告诉用户「初始化完成，请发送小说/剧本原始文本」。不得要求用户先给改编需求，也不得启动改编。
2. **原文归档 + 事件提取**：用户发送小说/剧本原始文本（txt/doc/docx 或明确的正文文件）后，必须在上述项目文件夹下创建原文 docx/文件链接并回贴；随后自动完成原文拆章/读取与事件提取，生成 `_cache/events.json`（覆盖当前 `chapter_range`，成功章数不得为 0）。事件提取是所有后续创作阶段（改编指引、故事大纲、集纲、剧本等）的共同前置门禁。默认不要求用户单独确认事件表，但若拆章失败、事件表缺失、事件提取失败或输出为空，必须阻断并告知用户，禁止进入任何创作产出。完成后告诉用户「原文已归档并完成事件提取，请给我改编需求，我先确认需求」。不得把原文塞进 dashboard，不写入表1，不启动改编。
3. **需求确认**：用户再提出改编需求，例如「把这部小说改成15集短剧，风格都市异能，每集1.5分钟，9:16，请先确认需求，不要直接跑」。此时只整理并确认需求，等用户明确确认后，才进入 adaptation_guidance + adaptation_review 第一阶段。

## 流程

项目初始化（创建/复用群名项目文件夹 + 表1:业务产物一览 + 表2：tokens消耗明细表，并回贴 3 个链接） → 归档原文 txt 为项目目录 docx 链接 → 确认改编需求 → adaptation_guidance → review gate → 用户回帖确认 → story_outline → review gate → 用户回帖确认 → jigang(集纲) → review gate → 用户回帖确认 → script → export

⚠️ **初始化必须在任何原文归档和改编产出前完成**。新项目若收到原文或改编需求但尚未初始化，先停下来执行项目初始化；初始化成功后必须在主群直接回贴项目文件夹、表1、表2三个可点击链接，并提示用户按下一步发送原文/补充需求。不得先跑改编指引、大纲、集纲或剧本。

⚠️ **不可跳步**。每个阶段产出独立文件（`adaptation_strategy.md` / `story_outline.md` / `story_skeleton.md`），业务组长可单独提取审核，同时沉淀为可复用过程资产（改编指引→同类项目参考、故事大纲→投流素材、集纲→分镜依据）。

🚫 **阶段推进硬门禁**：当前阶段文档和当前阶段审核文件必须先完成“创建/更新 → 写入正文 → 读回验证正文存在 → 在当前主群成功回贴链接 → 等待用户明确确认”五步，才允许进入下一阶段。若用户未看到回帖、链接无法打开、文档显示删除/无权限、或用户未明确确认，则必须停在当前阶段修复交付，禁止强行跑 story_outline / skeleton / script。

首阶段特别要求：`adaptation_strategy.md` 与 `reviews/adaptation_review.md` 必须作为第一组交付物单独回贴并等待确认；不得把它们混在后续大纲/集纲/剧本链接里一次性补发后继续推进。

子技能：`adam-adaptation`（改编指引）、`adam-supervision`（审核），由 `adam-drama` 编排调用。

整体大节点：原文+改编idea → 改编指引 → 新故事大纲 → 集纲 → 剧本全文。
每个阶段产出的文件路径必须明确告知用户；飞书环境下必须同步为在线文档/附件并回贴链接，不能只发消息卡片或路径。

## 飞书交付门禁（硬规则）

0. **原文事件读取是所有创作产出的内部前置门禁**：`adaptation_guidance` / `adaptation_strategy.md`、`story_outline.md`、`story_skeleton.md`、剧本正文等任何创作产出都不是凭需求直接生成，必须先完成原文读取与事件提取（例如生成 `_cache/events.json`，覆盖当前确认的 `chapter_range`，且成功章数不得为 0）。若原文拆章失败、事件表缺失、事件提取失败或输出为空，必须阻断并告知用户，禁止生成改编指引/故事大纲/集纲/剧本等任何创作产出。事件提取默认在原文归档后自动完成，不是一个需要用户确认的独立外部 review gate；用户明确要求“先看事件表/分析这本书”时，事件表也要按阶段产出同步飞书、读回验证并回贴主群。
1. **产出即同步**：每个阶段产出后必须立即进入 `export/sync` 交付步骤；没有在线文档链接/附件的阶段不算完成。
2. **回帖成功才算交付**：阶段文档/审核报告不仅要生成在线文档，还必须在当前飞书主群成功回贴链接；“工具创建成功但群里没出现回帖”“回帖链接打不开/显示已删除/无权限”都视为交付失败，必须先修复，不得进入下一阶段。
3. **必须停等确认**：每个 review gate 后必须等待用户明确确认（如“确认改编指引/继续下一阶段”）。没有用户确认，不得自动继续 story_outline、集纲、剧本。
4. **禁止只发卡片摘要**：飞书群里只发“已完成/摘要/路径”视为交付失败，必须补发在线文档链接或文件。
5. **阶段拆文档，不塞超长主文档**：不要把改编指引、大纲、集纲、全剧本全部塞入一个主文档。
6. **项目信息文档不放原文**：只放项目元信息、rules、阶段链接索引、进度、版本记录、下一步；禁止放原文正文。
7. **剧本按批次文档交付 + 必停确认**：默认 5 集一档，如 `EP001-005`、`EP006-010`。每完成一个批次后必须：保存/校验 → 同步飞书批次文档 → 回贴批次链接 → 停下来等待编剧/用户确认；不得自动生成「剧本汇总」，只有用户明确说“汇总/生成剧本汇总/剧本完结汇总”时才生成汇总文档；不得自动继续下一批，除非用户明确说“继续/确认下一批/全量自动跑”。修改单集时只更新所在批次/对应块，禁止全量重写全部剧本。
8. **长任务状态回贴**：后台任务/长命令不能只让用户看到 `Command still running`。关键节点必须直接在主群同步：批次保存完成、飞书同步完成、在线链接、等待确认、失败/阻塞原因；若后台已完成，必须补发完成状态和下一步，不得让旧状态误导用户。
9. **剧本汇总只在用户明确要求时生成，且只汇总剧本正文，禁止链接目录**：批次生成不需要、也不得自动生成「剧本汇总」；只有用户明确说“汇总/生成剧本汇总/剧本完结汇总”等汇总请求时，才生成「剧本汇总」。汇总文档只汇总“剧本”分批文档的正文内容，因为剧本按批次生成需要合并阅读；必须把所有 script batch 正文粘贴进同一个飞书文档。禁止把各 batch 的链接贴在同一个「剧本汇总」文档里当目录；批次链接只能出现在项目信息/状态索引，不得替代正文合并稿。不得把改编指引、改编审核、故事大纲、大纲审核、集纲、集纲审核等所有交付物都塞进最后汇总。非剧本交付物必须在各自阶段直接回贴链接并停顿确认。
10. **新项目群初始化**：表1/表2模板已沉淀在 `references/feishu_project_table_templates.json`（机器 schema）和 `references/feishu_project_table_templates.md`（人工说明），执行时以这两个文件 + `skills/drama/tools/feishu_project_executor.py` 的强约束为准。Fangcun 进入新项目群/新剧本项目时，飞书同步第一步必须在人马剧本库节点 `https://m9cfu49348.feishu.cn/wiki/NNfBwb32SiOhAwkrhRlcYkojnJg` 下，**抓取当前飞书群名**并按群名创建同名项目文件夹（例：群名 `某某短剧项目` → `某某短剧项目文件夹`）；禁止默认用剧名/小说名替代群名，除非拿不到群名才降级。在该项目文件夹下创建/复用两张多维表：`表1:业务产物一览`（只参考模板 `https://m9cfu49348.feishu.cn/base/NZ8vbVqpbajZqYsZuDjchf5mnvh?table=tblacd7rgShbaNBl&view=vewHS5ZPEB`，不要复制已填写旧表内容）和 `表2：tokens消耗明细表`（必须严格按模板 `https://m9cfu49348.feishu.cn/base/OIscbOQJQaf7MasVgsGccyrjndc?table=tblyC8Ee79rJXgCk&view=vewfNlNv1I` 一模一样初始化）。表1初始化时必须严格遵循模板字段顺序 `字段 / 内容 / 内容链接 / 来源说明 / 类型 / 时间`，其中 `内容链接` 为 URL/链接字段，必须位于 `内容` 与 `来源说明` 之间；同时清理 wiki 默认/多余列：删除 `Single option`、`Date`、`阶段`、`Attachment/Attatchment`、`备注`；写入前必须删除 wiki/bitable 自动生成的空白记录，表1不允许保留空行；从第 1 行开始写入：第 1 行 `编剧`（内容为“请填写编剧姓名”）、第 2 行 `BOT ID`（内容自动填当前 bot/account id）、第 3 行 `剧名`（内容自动填项目名称），三行的 `时间` 字段统一填建表时间；从第 4 行开始在 `字段` 列依次写入：`小说/剧本原文`、`改编指引`、`改编审核`、`故事大纲`、`大纲审核`、`集纲`、`集纲审核`、`剧本汇总`；固定模板不保留通用 `剧本` / `剧本审核` 占位行，剧本分批时由 Jingwei 在 `剧本汇总` 上方动态插入 `剧本EPxxx-yyy` / `剧本审核EPxxx-yyy` 行。其中第 4 行 `类型` 为 `用户输入`，其余产物行 `类型` 均为 `bot输出`，`来源说明` 均为 `bot自动抓取`。

12. **分发/新 bot 安装硬约束**：Fangcun skill 分发包不得携带任何落地项目目录、一次性测试 wrapper、运行 state、`__pycache__`、`.pyc`、具体项目名称/文件 token/表 token、开发 bot account/open_id。新 bot 安装后首次初始化必须重新在知识库根节点 `NNfBwb32SiOhAwkrhRlcYkojnJg` 下按当前飞书群名创建/复用项目文件夹，并生成本 bot 自己的 `projects/<slug>/drama/config.json` 与 `feishu_sync_state.json`，供 Jingwei 后续消费。

11. **链接回贴必须可直接点击**：初始化完成后必须把项目文件夹、表1、表2这 3 个路径作为可点击链接直接回贴主群；禁止把链接放进 Markdown 代码块或纯 JSON/日志块里。推荐格式：`项目文件夹：[标题](URL)`、`表1：[表1:业务产物一览](URL)`、`表2：[表2：tokens消耗明细表](URL)`；若当前渠道不稳定支持 Markdown，则同时保留明文 URL，但仍不得使用代码块。
12. **小说 txt 归档规则**：完成项目目录 + 表1 + 表2 后，一旦用户在群里发送小说/剧本原文 txt，必须立即把 txt 内容创建为项目目录下的飞书 docx 链接（不要塞进项目信息 dashboard），并在当前群聊直接回贴可点击链接给用户；**不需要、也不要把文件链接写入表1**。推荐工具：`skills/drama/tools/archive_source_txt.py --file <txt> --config <project>/drama/config.json --account <当前bot>`。
13. **项目文件夹强约束**：改编指引、改编审核报告、故事大纲、集纲、剧本批次、Dashboard 等后续文件必须创建在该同名项目文件夹下；不得散落到根目录或其他文件夹。`feishu_sync_state.json` 必须记录 `project_workspace`、项目文件夹 node、两张表 app/table 信息。
14. **本地路径必须相对当前 workspace/配置自动推导**：不同 OpenClaw 代际路径不同（例如 claw2/4/5 常见 `/root/.openclaw/workspaces/<bot_id>/`，claw3 常见 `/root/.openclaw/agents/<bot_id>/workspace/`）。Fangcun 代码和文档不得写死这些绝对路径，也不得写死旧 bot 路径；所有项目状态、`projects/<slug>/drama/config.json`、`feishu_sync_state.json`、产物输出目录都必须基于当前进程工作目录、传入的 `--config`、或 `Path(__file__).resolve()` 自动定位。
14. **命名规范与版本保留**：每个阶段文档标题必须带版本号，如 `《项目名》-故事大纲-V1`、`《项目名》-故事大纲-V2` 或 `《项目名》-故事大纲-vYYYYMMDD-r1`。默认每次新生成都创建新版本文档并永久保留旧版本；不得默认覆盖、清空、删除或自动重命名旧文档。只有用户明确说“覆盖原文档/直接更新当前版本/patch 当前文档”并传 `--overwrite-current` 时，才允许 update 当前推荐版本。
15. **自动执行器闭环**：飞书同步必须优先运行 `python tools/build_feishu_project.py --config <config.json> --execute --account <当前bot账号>`，由程序自动初始化项目文件夹、创建两张项目表、create 新版本 docx、保存 `<output_dir>/feishu_sync_state.json` 并打印链接；禁止只生成 manifest 后等 agent 手动调工具。
16. **事件表禁止用 Markdown 表格同步到飞书**：事件提取结果即使本地是单行 `|...|` 表格，也必须在 `build_feishu_project.py` 中转成普通列表字段（章节/人物/事件/戏剧强度/改编风险/建议时长/类型）再写 docx；禁止依赖飞书 docx markdown 表格转换，避免文档只剩空标题/“表格内容”。同步后必须读回或抽检确认事件正文存在。
17. **飞书群聊禁止 Thread**：所有链接、文件、确认询问直接发主群。
18. **飞书交付禁止代码块**：fangcun 转化/同步到飞书的所有在线文档正文一律不得出现 Markdown 代码围栏/代码块（例如 ```、```markdown、```json、```text）。包括“小说/剧本原文”、改编指引、改编审核、故事大纲、大纲审核、集纲、集纲审核、剧本批次、剧本审核、完整导出、事件表和 dashboard。原始 txt/md/json 中若带有代码围栏，写入飞书前必须自动剥离围栏，只保留内部正文；读回发现代码块即视为交付失败，必须修复后再回贴链接/推进阶段。
19. **表格不承载文件链接**：Fangcun 目前负责建项目文件夹、建立表格、初始化表格数据（bot_id、剧名、固定产物项等）、创建小说/剧本原文链接，并把后续生成物放入项目目录；更改要求：**不需要把任何文件链接贴入表格**。表1/表2只做项目初始化与信息登记，文件链接通过群聊回贴和项目目录查看。
20. **资产 Dashboard / 剧本汇总按需生成**：资产 Dashboard 不随剧本批次自动生成飞书文档；普通批次同步只登记本地资产 registry、同步 `剧本EPxxx-yyy` 批次文档并回贴批次链接。只有用户明确说“汇总/生成剧本汇总/剧本完结汇总”等汇总请求时，才同步标题为「剧本汇总」的飞书文档，保证 Jingwei 能贴到表1的「剧本汇总」行。该「剧本汇总」文档正文必须由所有已同步 script batch 的剧本正文合并生成，禁止写成 batch 链接列表/当前推荐版本目录；生成前必须检查每个已登记 `script_batch:EPxxx-yyy` 的 `active_version`，确保汇总覆盖所有最新批次，缺任何最新批次都必须阻断。程序还需用正文特征（分集标识、场次、△动作）做门禁。不得在改编指引、大纲、集纲、各阶段审核或普通剧本批次同步时自动生成 Dashboard/剧本汇总。若因跳步/误同步产生提前的大纲、集纲、审核或链接目录式 Dashboard，必须删除在线文档并清理本地草稿/状态，不能作为有效门禁结果。

## 后续能力规划

编剧 Agent 后续需求（文本资产管理、token 统计、台词翻译/风格改写）统一记录在 `ROADMAP.md`。这些属于增量规划，不影响当前主流程；实现时应优先保持飞书交付闭环和项目资产沉淀。

## 路由

| 用户说 | 处理 |
|--------|------|
| "用fangcun" / "fangcun项目初始化" / "初始化项目" | 只执行项目初始化：抓群名建项目文件夹 + 表1 + 表2，回贴链接并提示发送原文 |
| 用户发送小说/剧本原始文本文件 | 只归档原文到项目文件夹，回贴原文链接并提示给改编需求 |
| "写剧本" / "改编短剧" / "小说转剧本" | 若未初始化先初始化；若未归档原文先要求原文；若二者齐备则先确认需求，确认后进入 pipeline |
| "分析这本书" / "提取事件" / "生成骨架" | 对应阶段；仍必须满足初始化、原文归档、需求确认门禁 |
| "写前N集" / "继续写" | 剧本编写 |
| "看审核报告" | 读取审核结果 |
| "记成项目规则：…" / "把…加入当前项目 rules" / "记一下这个项目…" / "把这个记一下" / "加到 rules" | 直接写入项目规则并升版本 |
| "帮我从最近群聊里提取当前项目 rules" / "整理最近群里定下来的规则" / "总结 rules" / "提取 rules" / "整理 rules" / "总结一下这个项目的要求" | 自动识别当前剧本 → 过滤非剧本讨论 → 提取候选规则 |
| "采纳 1,2,4" / "全部采纳" / "只采纳硬规则" / "都加进去" | 将候选规则正式入库 |

## 平台

Claude Code / Codex / Cursor / OpenCode / Gemini CLI

## 运行时

Python 3.10+ (需 requests 库) + Shell 执行 + 文件读写

## 子代理

| 代理 | 用途 |
|------|------|
| drama-director | 决策层：需求分析、任务拆解、质量门控 |
| quality-supervisor | 监督层：A/B/C/D 评分审核 |
| skeleton-builder | 执行层：故事骨架搭建 |
| adaptation-strategist | 执行层：改编策略制定 |
| script-writer | 执行层：情绪优先剧本编写 |
| event-extractor | 执行层：事件提取 |

## 自动激活

SessionStart Hook 检测 _cache/events.json 或 drama/ 目录自动激活。

## 项目 Rules 自然语言触发

当用户在群里直接说下列话术时，agent 应自动路由到项目 rules 处理，而不是要求用户记 CLI：

### 1. 直接入库型（最短可说法）
- 「记一下：不要反差」
- 「把这个记一下」
- 「加到 rules：女主不要降智」
- 「记成规则：每集结尾都要有钩子」
- 「从现在开始，这个项目不要强反转堆叠」

处理：等价于 `--add-project-rule`，直接入库、升版本、后续阶段立即生效。

### 2. 群聊提取型（超级 easy 短触发）
- 「总结 rules」
- 「提取 rules」
- 「整理 rules」
- 「总结一下这个项目的要求」
- 「帮我从最近群聊里提取当前项目 rules」

处理：
1. 先识别本群当前最近在聊的剧本项目
2. 过滤 fangcun 优化讨论 / 系统机制 / bug 状态 / 群治理内容
3. 只提取该剧本的创作相关规则
4. 返回候选清单等待确认

### 3. 候选确认型（最短可说法）
- 「采纳 1,2,4」
- 「全部采纳」
- 「只采纳硬规则」
- 「都加进去」

处理：把候选规则正式写入 `project_rules.md/json`，自动升版本，并回贴规则变更摘要。

## 上层接线约定（必须按这个体验做）

业务侧不应该看到 CLI。上层 agent 遇到以下短触发词时，应自动路由到 rules 流程：

- `总结 rules` / `提取 rules` / `整理 rules`
  - 自动走：当前剧本识别 → 非剧本讨论过滤 → 候选规则提取
- `全部采纳` / `采纳 1,2,4` / `只采纳硬规则` / `都加进去`
  - 自动走：候选规则确认入库
- `记一下：...` / `加到 rules：...` / `记成规则：...`
  - 自动走：直接正式入库

**目标体验：** 用户只说一句「总结 rules」，就直接进入流程，不要先解释命令，不要弹说明书。

- 表2必须严格按模板 `OIscbOQJQaf7MasVgsGccyrjndc/tblyC8Ee79rJXgCk` 一模一样初始化：字段为 `序号 / usage_date / person / bot_id / chat_id / project / model_provider / model / input_tokens / output_tokens / total_tokens / 来源说明`；其中 `来源说明` 必须是最后一列单选字段，选项为 `用户触发`、`自动触发`；禁止出现任何 `费用`、`费用估算`、`成本` 类字段或内容。
